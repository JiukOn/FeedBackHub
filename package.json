{
  "name": "feedback-hub-gerente-bionico",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://JiukOn.github.io/FeedBackHub",
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-keywords": "^5.1.0",
    "dompurify": "^3.4.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "uuid": "^9.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "verify": "node scripts/verify.js",
    "deploy": "npm run build && npx gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
